let quests = { goblins: 0, ore: 0, wood: 0 };
async function saveQuestsToCloud() { if (!supabaseClient || !currentPlayer.id || !isLoggedIn) return; await supabaseClient.from("players").update({ quest_data: JSON.stringify(quests) }).eq("id", currentPlayer.id); }
function loadQuestsFromCloud(qData) { try { if (qData && qData !== '{}') quests = JSON.parse(qData); else quests = { goblins:0, ore:0, wood:0 }; updateUI(); } catch(e) { quests = { goblins:0, ore:0, wood:0 }; } }
function updateQuests(type, amount) { if (type === 'goblin') quests.goblins = Math.min(5, quests.goblins + amount); if (type === 'ore') quests.ore = Math.min(10, quests.ore + amount); if (type === 'wood') quests.wood = Math.min(10, quests.wood + amount); updateUI(); saveQuestsToCloud(); let completed = false; if (quests.goblins >= 5) { addTechnicalLog("📜 Квест 'Убить гоблинов' выполнен! +50 опыта, +30 монет"); currentPlayer.exp += 50; currentPlayer.gold += 30; completed = true; } if (quests.ore >= 10) { addTechnicalLog("📜 Квест 'Собрать руду' выполнен! +40 опыта, +50 монет"); currentPlayer.exp += 40; currentPlayer.gold += 50; completed = true; } if (quests.wood >= 10) { addTechnicalLog("📜 Квест 'Собрать древесину' выполнен! +40 опыта, +50 монет"); currentPlayer.exp += 40; currentPlayer.gold += 50; completed = true; } if (completed) { updateUI(); savePlayerToCloud(); saveQuestsToCloud(); } }